import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Malt = (props) => {
  return (
    <View>
      
    </View>
  );
};

export default Malt;

const styles = StyleSheet.create({});
